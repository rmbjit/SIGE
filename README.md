# SIGE

Repositório técnico temporário para a execução auditável da matriz CSP autenticada da Fase 10.

Nenhuma credencial é versionada. O canal de autenticação do workflow usa RSA-OAEP-SHA256 efémero e é destruído após a execução.
