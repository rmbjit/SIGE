DEPLOY - SIGE SoftGenial v12.12.55
Fase 4 (CSP e front-end seguro) - incremento 2
Infraestrutura de nonce CSP e nonce nas tags script inline
Data: 2026-06-23

NATUREZA DA ENTREGA
- Pacote instalavel completo (pasta sige-softgenial na raiz do ZIP).
- Vaga de preparacao para o CSP. Acrescenta um nonce por pedido e aplica-o as
  tags script inline das vistas. Inocuo enquanto o cabecalho CSP nao for
  activado: os navegadores ignoram o nonce sem politica CSP. Nao altera o
  comportamento das paginas.
- Nao toca em regras de calculo financeiro nem no ficheiro canonico finance-core.

PRE-REQUISITOS
- Sem alteracao de base de dados. Sem migracao. Sem alteracao de configuracao.
- Sem novas dependencias externas (mantem-se 9 hosts).

PASSOS
1. Fazer copia de seguranca do plugin actual (pasta wp-content/plugins/sige-softgenial).
2. Em alternativa recomendada, desactivar o plugin antes de substituir os ficheiros.
3. Substituir a pasta sige-softgenial pela desta entrega (pacote completo).
4. Reactivar o plugin, se tiver sido desactivado.
5. Confirmar a versao 12.12.55 no cabecalho do plugin.

VERIFICACAO POS-DEPLOY
- Abrir varias vistas administrativas (academico, financeiro, jardim, sistema,
  WhatsApp, recursos humanos) e confirmar que carregam e funcionam como antes.
- No HTML gerado, confirmar que as tags script inline destas vistas passaram a
  incluir um atributo nonce com um valor base64. O comportamento mantem-se,
  porque ainda nao ha cabecalho CSP a impor o nonce.
- Confirmar que os redireccionamentos (por exemplo apos gravar configuracoes ou
  encerrar periodos) continuam a funcionar normalmente: ficaram de fora desta
  vaga e serao tratados a seguir.
- Confirmar que a impressao e os documentos (modelos PDF, motor de documentos,
  camara da portaria) continuam a funcionar: ficaram de fora desta vaga.

NOTAS
- O nonce e gerado uma so vez por pedido e reutilizado, ja a preparar o futuro
  cabecalho CSP, que usara o mesmo nonce.
- Esta entrega nao activa nenhum cabecalho CSP. O cabecalho (primeiro em
  Report-Only e depois enforce) sera um incremento seguinte.
- A catraca de blocos script inline desceu de 81 para 25 e passou a exigir a
  infraestrutura de nonce; impede regressao em entregas futuras.

REVERSAO
- Repor a pasta a partir da copia de seguranca. Sem passos de base de dados.
