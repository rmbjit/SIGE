## v12.12.14 - Secret Vault, incremento 1: credenciais dos gateways cifradas em repouso (2026-06-20)

- Cofre de segredos unificado (includes/security-vault.php) construido sobre a cifra forte ja existente (sige_encrypt_token/sige_decrypt_token: sodium secretbox, com recuo para AES-256-GCM; chave derivada dos salts do WordPress; formatos com prefixo sige2: e gcm1:). Funcoes sige_vault_seal (cifra), sige_vault_reveal (decifra so os nossos formatos; texto em claro passa intacto), sige_vault_is_sealed, e um registo auditavel das chaves-segredo (sige_vault_secret_registry, sige_vault_is_secret_key).
- As credenciais dos gateways de pagamento passam a ser cifradas em repouso: M-Pesa (api_key, public_key), e-Mola (api_key, api_secret) e o webhook_token. Antes eram gravadas em claro nas opcoes. A selagem e transparente: os clientes M-Pesa/e-Mola continuam a receber o valor em claro, e o webhook (find_school_by_token) revela o token antes da comparacao. Sem alteracao ao comportamento dos pagamentos.
- Migracao retrocompativel sem operacao em massa: a leitura passa o texto em claro intacto (instalacoes pre-cofre continuam a funcionar), e ha auto-reparacao na leitura no admin (a primeira leitura de cada segredo regrava-o cifrado, uma so vez; nunca no caminho publico do webhook). Sem alteracao de esquema de base de dados.
- Registo de segredos auditavel: cataloga as credenciais dos gateways (cifradas nesta fase) e tambem a password SMTP e o token WhatsApp/Z-API (que ja eram cifrados), para o registo ser completo. Um gate verifica que nenhuma chave-segredo registada e gravada em claro.
- Redaccao confirmada: os ecras de configuracao dos gateways nao pre-preenchem o segredo (campos password sem valor; mostram apenas configurado/nao). Selar nunca perde o segredo (se a cifra falhar, mantem o original) e e idempotente.
- Sem novo endpoint: manifesto mantem-se em 196 e as regras do Kernel inalteradas. Sem alteracao de regras de calculo. Sem alteracao de permissoes de configuracao dos pagamentos. Zero travessoes em codigo e documentos.
- Governanca: gate check-vault.php e smoke smoke-vault-v12-12-14.php (26 verificacoes, incluindo a integracao real com os pagamentos e o webhook) ligados ao corredor. Rediagnostico adversarial Zero P0/P1; achados P3 e a decisao de adiar a rotacao de chave documentados.

